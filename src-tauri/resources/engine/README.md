The AudioSync analysis engine is built into this directory by
`npm run fetch-engine`; it is a build artifact and is not committed.

Without it, the "Measure delays" action is unavailable and the app reports
the engine as missing. Everything else works normally.
